interface:
  display_name: Browser CDP
  short_description: 默认使用 Codex 浏览器；仅在用户明确要求时走上游 CDP 兼容路径。
  default_prompt: "$browser-cdp 帮我用 Codex Browser 检查一个网页。"
policy:
  allow_implicit_invocation: false
